/**
 * Rendering pipeline framework including command buffers, render extensions,
 * adaptive path selection, double-buffer queues, lock-free ring buffers,
 * and async pipeline execution for the render graph.
 */
package com.ranecc.renderium.feature.pipeline;
