/**
 * Pipeline node abstractions providing the node interface, abstract base,
 * command model, and node registry for constructing the render graph.
 */
package com.ranecc.renderium.feature.shader.pipeline.node;
